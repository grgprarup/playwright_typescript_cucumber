import { Given, Then, When } from "@cucumber/cucumber";

Given('the user has browsed to the login page', function () {
    
  });

  When('the use logs in with username {string} and password {string} using the webUI', function (string, string2) {
   
  });

  Then('the user should be in homepage', function () {
   
  });
